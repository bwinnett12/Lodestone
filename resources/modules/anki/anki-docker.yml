services:
  anki-sync-server:
    image: chrislongros/anki-sync-server-enhanced
    container_name: anki-sync
    ports:
      - "3111:3111"   # Sync server (HTTP)
      - "8443:8443"   # Sync server (HTTPS, if TLS_ENABLED)
      - "3112:8081"   # Dashboard (optional)
      - "3113:9090"   # Metrics (optional)
    environment:
      - SYNC_USER1=alice:password1
      - TZ=America/Anchorage
      - BACKUP_ENABLED=true
      - METRICS_ENABLED=true
      - DASHBOARD_ENABLED=true
    volumes:
      - /storage/Orchid/shortstack/anki:/data
      - /storage/Orchid/backups/anki:/backups
    restart: unless-stopped
